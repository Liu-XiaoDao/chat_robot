<%= form_for @book, url: books_path, html: { class: "form-horizontal row-fluid", id: "create_book_form" }  do |f| %>
  <%= render 'form', f: f %>
  <%#= render partial: field_partial_of(@current_resource, 'form', field), locals: { f: f, field: field } %>
<% end %>
