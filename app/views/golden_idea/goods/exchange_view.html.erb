<%= form_for @good, url: exchange_golden_idea_good_path(@good), remote: true, html: { class: "form-horizontal row-fluid", id: "exchange_form" }  do |f| %>
  <div class="form-group row">
    <label for="inputEmail3" class="col-sm-2 col-form-label"><%= t 'form.exchange_quantity' %></label>
    <div class="col-sm-10">
      <%= text_field_tag :exchange_quantity, nil, {placeholder: "Type something here...", class: "form-control"} %>
    </div>
  </div>
<% end %>
