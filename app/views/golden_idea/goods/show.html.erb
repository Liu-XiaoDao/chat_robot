<div class="wrapper wrapper-library">
  <div class="container">
    <div class="row">
      <%= render 'golden_idea/application/left' %>
      <div class="span9">
        <%= render "golden_idea/application/flash" %>
        <div class="content">
          <div class="module">
            <div class="module-body">
              <div class="profile-head media">
                <a href="<%= @good.avatar.url %>" data-fancybox="gallery" class="media-cover pull-left fancybox">
                  <img src="<%= @good.avatar.url %>">
                </a>
                <div class="media-body">
                  <h4>
                    <%= @good.name %><small><%= t 'table.inventory' %>:<%= @good.quantity %>, <%= t 'table.point' %>:<%= @good.score %></small>
                  </h4>
                  <p class="profile-brief" title="<%= @good.description %>"><%= truncate(@good.description,length: 100) %></p>
                  <div class="profile-details muted">
                    <a href="<%= exchange_view_golden_idea_good_path %>" class="btn" data-toggle="modal" data-target="#myModal" ><i class="icon-shopping-cart shaded"></i><%= t 'table.exchange' %></a>
                  </div>
                </div>
              </div>
              <ul class="profile-tab nav nav-tabs">
                <li class="active"><a href="#detial" data-toggle="tab"><%= t 'table.detail' %></a></li>
                <li><a href="#borrow_records" data-toggle="tab"><%= t 'table.exchange_record' %></a></li>
              </ul>
              <div class="profile-tab-content tab-content">
                <div class="tab-pane fade active in" id="detial">
                   <div class="module-body" style="">
                     <%= @good.content.html_safe %>
                  </div>
                </div>

                <div class="tab-pane fade" id="borrow_records">
                  <div class="module-body">
                    <table class="table table-striped">
                      <thead>
                        <tr>
                          <th>#</th>
                          <th><%= t 'table.exchange_person' %></th>
                          <th><%= t 'table.count' %></th>
                          <th><%= t 'table.date' %></th>
                        </tr>
                      </thead>
                      <tbody>
                        <% @good.exchange_records.each do |exchange_record| %>
                          <tr>
                            <td><%= exchange_record.id %></td>
                            <td><%= exchange_record.employee.name %></td>
                            <td><%= exchange_record.quantity %></td>
                            <td><%= exchange_record.created_at %></td>
                          </tr>
                        <% end %>
                      </tbody>
                    </table>
                  </div>
                </div>

              </div>
            </div>
          </div>
        </div>
      </div><!--/.span9-->

    </div>
  </div><!--/.container-->
</div><!--/.wrapper-->

<!--兑换弹框-->
<%= render 'modal_exchange' %>
